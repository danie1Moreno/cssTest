/* import * as d3 from 'd3';
import { useEffect, useRef, useState } from 'react';

export const useStreamgraph = ({value}) => {
    const svgRef = useRef(null);
  return (
    <div>useStreamgraph</div>
  )
} */
